# Logo Upload Directory

Upload your logo here and name it `logo.png`, `logo.svg`, or `logo.jpg`.
Then, update the `Header.tsx` and `Footer.tsx` files to match the file extension if it's not `logo.png`. By default, the application is looking for `/logo.png`.
